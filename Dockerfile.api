FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
COPY common ./common
COPY mcp_backend ./mcp_backend
COPY rest_api ./rest_api

RUN pip install --no-cache-dir -e .

EXPOSE 8080

CMD ["sh", "-c", "uvicorn rest_api.main:app --host 0.0.0.0 --port ${PORT:-8080}"]
